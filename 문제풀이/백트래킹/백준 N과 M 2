
def dfs(start):
    if len(result) == M:
        print(' '.join(map(str,result)))
        return
    
    for i in range(start, N+1):
        if i not in result:
            result.append(i)
            dfs(i)
            result.pop()


N,M = map(int, input().split())
result = []

dfs(1)

#입력
#4 2